process {
    withName: 'NGS_BITS' {
        publishDir = [
        path: { "${params.outdir}/mapping_qc/ngs_bits/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
