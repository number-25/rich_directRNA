process {
    withName: 'OARFISH' {
        publishDir = [
        path: { "${params.outdir}/transcript_quantification/oarfish/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
