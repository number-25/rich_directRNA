process {
    withName: 'SAMTOOLS_FLAGSTAT' {
        publishDir = [
        path: { "${params.outdir}/mapping_qc/samtools_flagstat/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
