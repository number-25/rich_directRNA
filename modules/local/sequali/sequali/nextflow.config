process {
    withName: 'SEQUALI' {
        publishDir = [
        path: { "${params.outdir}/fastq_qc/sequali/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
