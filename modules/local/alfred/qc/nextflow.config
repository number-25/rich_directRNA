process {
    withName: 'ALFRED' {
        publishDir = [
        path: { "${params.outdir}/mapping_qc/alfred/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
