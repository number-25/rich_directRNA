process {
    withName: 'CRAMINO' {
        publishDir = [
        path: { "${params.outdir}/mapping_qc/cramino/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
