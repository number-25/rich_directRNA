process {
    withName: 'STRINGTIE' {
        publishDir = [
        path: { "${params.outdir}/transcript_reconstruction/stringtie/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
