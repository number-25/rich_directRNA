process {
    withName: 'FLAIR_COLLAPSE' {
        publishDir = [
        path: { "${params.outdir}/transcript_reconstruction/flair/collapse/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
