process {
    withName: 'FLAIR_CORRECT' {
        publishDir = [
        path: { "${params.outdir}/transcript_reconstruction/flair/correct/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
