process {
    withName: 'JAFFAL' {
        publishDir = [
        path: { "${params.outdir}/fusion_genes/jaffal/${meta.id}_${meta.replicate}" },
        //mode: params.publish_dir_mode
        ]
    }
}
